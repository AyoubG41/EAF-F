{% extends 'base.html.twig' %}

{% block title %}EvaluationElements{% endblock %}

{% block body %}
    <h1>EvaluationElements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation_element.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evaluation_element.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_elements_index') }}">back to list</a>

    <a href="{{ path('evaluation_elements_edit', {'id': evaluation_element.id}) }}">edit</a>

    {{ include('evaluation_elements/_delete_form.html.twig') }}
{% endblock %}
