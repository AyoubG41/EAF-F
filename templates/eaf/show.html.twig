{% extends 'base.html.twig' %}

{% block title %}EAF{% endblock %}

{% block body %}
    <h1>EAF</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ e_a_f.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ e_a_f.name }}</td>
            </tr>
            <tr>
                <th>Version</th>
                <td>{{ e_a_f.version }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('e_a_f_index') }}">back to list</a>

    <a href="{{ path('e_a_f_edit', {'id': e_a_f.id}) }}">edit</a>

    {{ include('eaf/_delete_form.html.twig') }}
{% endblock %}
